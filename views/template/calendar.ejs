<%- include("template/header") %>

<div id="calendar">
    <div class="calendar-header">
        <span class="month-year"></span>
        <button id="prev-month">Previous</button>
        <button id="next-month">Next</button>
    </div>
    <div id="event-modal" style="display:none;">
        <form id="event-form">
            <label for="event-date">Date:</label>
            <input type="date" id="event-date" name="date" required>
            
            <label for="event-game">Game:</label>
            <select id="event-game" name="game" class="dropdown" required>
                <option value="chess">Chess</option>
                <option value="checkers">Checkers</option>
                <option value="jigsaw">Jigsaw</option>
                <option value="bingo">Bingo</option>
                <option value="sudoku">Sudoku</option>
            </select>
            
            <label for="event-participants">Participants:</label>
            <select id="event-participants" name="participants" class="dropdown" multiple required></select>
            
            <button type="submit">Create Event</button>
        </form>
    </div>
    <div class="calendar-body">
        <div class="day-names">
            <div>Sun</div>
            <div>Mon</div>
            <div>Tue</div>
            <div>Wed</div>
            <div>Thu</div>
            <div>Fri</div>
            <div>Sat</div>
        </div>
        <div class="dates" id="dates"></div>
    </div>
</div>

<br>
<br>
<br>

<script>
  const userId = "<%= userId %>";
  const userEmail = "<%= userEmail %>";
  console.log("UserId:", userId);
  console.log("UserEmail:", userEmail);
</script>
<script src="/js/calendar.js"></script>

<%- include("template/footer") %>
